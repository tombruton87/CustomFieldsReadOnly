# CustomFieldsReadOnly
Extends FreeScout's Custom Fields module to mark fields as API-only (read-only in the UI) or hidden from the ticket view. Protects fields managed via the API from accidental edits, with server-side middleware enforcement. Requires the Custom Fields module.
